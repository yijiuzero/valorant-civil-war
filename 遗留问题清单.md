# 遗留问题清单（截至 V4.1.1）

> 仅列 **尚未修复** 的问题。功能性硬 bug（静默登录失效、分队不同步、刷新丢房主、老账号下拉残留、内鬼段位一致）已在 V4.0.0 / V4.1.1 修复，不在本清单。

## 一、安全问题（安全类）

### 1. RLS 完全信任前端 ⚠️ 高危 ·【V4.2.7 已修复】
- **部位**：`init-spy-db.sql:31`（rooms 表 UPDATE 策略）
- **现状**：`USING (auth.role() = 'authenticated')` —— 任意已登录用户都能改任意房间的字段（host_user_id、spy_state、status 等）。
- **风险**：配合暴露的 publishable key，任何人可直接调 Supabase API 抢房主、篡改内鬼结果、踢光所有人。
- **修复建议**：改为 `USING (auth.uid() = host_user_id) WITH CHECK (auth.uid() = host_user_id)`（文件第 49 行注释里已有现成写法）。
- **我的建议**：**必改**，但属于 Supabase 后端策略变更 + 需重新执行 SQL 部署，纯前端兜不住；动之前我会先给你完整方案确认，避免影响线上房间。
- **成本/风险**：中（数据库变更，需评估是否破坏现有前端逻辑）。
- **【V4.2.7 修复】**：原"任意登录用户改任意房间"已在 V4.1.2/13 动过 SQL（host/status/code/spy_state 受触发器保护），但漏了 `created_at` 字段；V4.2.7 在触发器补 `created_at` 禁止改动检查，彻底堵住"改 created_at + 删任意 lobby 房间"的攻击链。**需到 Supabase 重新执行 `init-spy-db.sql` 生效。**

### 2. 注册账户枚举 🟢 低危 ·【V4.2.4 已修复】
- **部位**：`auth.js:160-163`
- **现状**：注册时若昵称已存在，自动 `doSignIn` 并尝试登录，等于告诉别人「这昵称已注册」。
- **修复建议**：统一成中性提示、不再自动登录（或保留自动登录但用模糊提示）。
- **我的建议**：**可不动**。当前是「自动登录便利 vs 防枚举」的权衡，之前选了保留自动登录；改了会失去「已注册直接登录」的体验。
- **成本/风险**：低。
- **【V4.2.4 已修复】**：注册/登录错误提示已统一为「昵称或密码错误」（`auth.js` 的 `handleAuthSubmit` catch 分支），不再通过响应差异暴露账户是否存在。

## 二、功能隐患（功能类）

### 3. 内鬼模式用玩家名（display_name）当唯一标识 🟡 中危 · 建议改
- **部位**：`spy-mode.js:397 updatePlayerTeam(name, team)`、`:412-414` 随机选内鬼、`:350-351` 任务按 name 查、`:331` 内鬼身份按 name 判定
- **现状**：`spy_state.players` 用 `name` 做 key。两个重名玩家同房间时，任务指派 / 内鬼身份会串（A 的任务可能显示给 B，或内鬼判定错）。
- **修复建议**：改用 `user_id` 当唯一标识（内战 `players` 表本来就用 user_id），`tasks`、内鬼指派、身份判定全部走 user_id。
- **我的建议**：**建议改**，但涉及 teamsplit→内鬼桥接和 spy 两端标识逻辑，有回归风险，需谨慎测试。
- **成本/风险**：中。

### 4. 两套内鬼 state schema 不兼容 🟡 中危 · 建议改（维护性）
- **部位**：`spy-mode.js:91`（桥接路径写 `team_a_spy: spyA.id`）、`:191/413`（Lobby 路径写 `team_a_spy_name`，按 name）
- **现状**：teamsplit 桥接路径内鬼用 player **id**，在线 Lobby 路径内鬼用玩家 **name**。`renderSpyRevealed`（:91/:140）和 Lobby 视图（:331/:350）各认一种，长期易踩坑。
- **修复建议**：统一成一种（推荐 user_id）。
- **我的建议**：**建议改**，与 #3 一起做最好（统一标识顺手解决）。
- **成本/风险**：中。

### 5. 内鬼房主用 display_name 判定 🟢 低危 ·【V4.2.3 已修复】
- **部位**：`spy-mode.js:230` 附近 `host_name === window._currentUserDisplayName`
- **现状**：在线 Lobby 用昵称判定房主；重名或用户改 display_name 后会错认/漏认房主。
- **修复建议**：改用 `user_id` 判定（与内战一致）。
- **我的建议**：**可不动**（与 #3/#4 同源，若动 #3/#4 顺手修掉）；单独改收益低。
- **成本/风险**：低。
- **【V4.2.3 已修复】**：`isHost()` 已改用 `lobbyState.host_user_id === window._currentUser.id`（user_id 比较），桥接路径 `isSpyHost()` 用 `spyUserId === spyHostUserId`。重名/改昵称不再错认房主。

## 三、可维护性与技术债（非功能，不影响使用）

### 6. 双 Supabase 实例 🟢 低危 ·【V4.2.7 已修复】
- **部位**：`teamsplit.js:3` 自建 `createClient`；`app-data.js`/`auth.js`/`spy-mode.js` 用 `_getSupabase()`
- **现状**：两套客户端各自连一次，浪费连接。
- **修复建议**：统一用 `_getSupabase()`。
- **我的建议**：**可不动**，纯可维护性，无功能影响。
- **成本/风险**：低。
- **【V4.2.7 已修复】**：`teamsplit.js` 已统一复用全局 `window._getSupabase()`（top-level await），移除自建 `createClient` 与 `signInAnonymously` 匿名 fallback；与 auth.js / spy-mode.js 共享同一实例与 session，房主身份判定不再有双源不一致风险。

### 7. 切模块未退订 Realtime 频道 🟢 低危 · 可不动
- **部位**：`switchModule`（index.html / app.js）无 `unsubscribe`
- **现状**：teamsplit / spy 频道切换模块后没退订，后台一直占连接。
- **修复建议**：`switchModule` 里 `roomSubscription?.unsubscribe()` / `leaveSpyMode()`。
- **我的建议**：**可不动**，多房间/多模块长跑才显影响。
- **成本/风险**：低。

### 8. 死代码 🟢 极低 · 可不动
- **部位**：`teamsplit.js:250 joinLobby()`、`index.html:131 playerNameInput`（`display:none` 且 `value="auto"`）、`index.html:144 btnJoinLobby`（`display:none`）
- **现状**：`autoJoinLobby` 已接管，这些永不被触发；`playerNameInput` 误用时还会建出名叫「auto」的玩家。
- **修复建议**：删除死代码 / 隐藏输入框。
- **我的建议**：**可不动**，删了更干净，但有极小回归风险（万一哪条路径还引用），属纯清理。
- **成本/风险**：极低。

### 9. 缺 CSP 🟢 低危 · 不改（评估过）
- **部位**：`index.html` 无 `<meta http-equiv="Content-Security-Policy">`
- **现状**：页面大量 `innerHTML` 拼接 + 内联 `onclick`、引入外链 CDN，无 CSP。
- **修复建议**：加 CSP meta。
- **我的建议**：**不改**。页面内联 onclick / 内联样式过多，严格 CSP 会直接把整站打挂；且 XSS 入口已通过 `esc()` / 房间码清洗堵住，优先级低。
- **成本/风险**：高（易误伤现有功能）。

---

## 我的总判断
- **真该修的只有 #1（RLS）** —— 这是唯一高危安全漏洞，但要动 Supabase 后端，得你拍板 + 我出部署方案。
- **#3 / #4（内鬼重名 + 双 schema）** 建议择机一起修，属中危功能隐患，触发条件（同房间重名）不算常见但确实存在。
- **#2 / #5 / #6 / #7 / #8 / #9** 都可不动，是产品权衡或纯技术债，不影响当前使用。

要动哪个，你定。
